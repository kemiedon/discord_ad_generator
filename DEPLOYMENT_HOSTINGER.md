# Hostinger 部署指南

## 📦 部署前準備

### 1. 建置專案

```bash
npm run build
```

建置完成後會生成 `dist` 資料夾，包含以下檔案：

- `index.html` - 主頁面
- `assets/` - CSS、JS、圖片等靜態資源

---

## 🌐 Hostinger 部署步驟

### 方法一：使用 File Manager（推薦新手）

#### Step 1: 登入 Hostinger

1. 前往 [Hostinger](https://www.hostinger.com)
2. 登入你的帳號
3. 進入 **控制面板 (hPanel)**

#### Step 2: 找到 File Manager

1. 在 hPanel 中找到 **檔案 (Files)** 區塊
2. 點擊 **File Manager（檔案管理員）**
3. 選擇你的網域

#### Step 3: 清空 public_html 資料夾

1. 進入 `public_html` 資料夾
2. 刪除所有預設檔案（如 `index.html`、`default.php` 等）
   - 選取所有檔案 → 點擊 **Delete**

#### Step 4: 上傳 dist 資料夾內容

1. 確保你在 `public_html` 資料夾中
2. 點擊 **Upload Files（上傳檔案）**
3. 選擇 `dist` 資料夾內的**所有檔案和資料夾**：
   - `index.html`
   - `assets` 資料夾（整個資料夾）
   - `skill_hub_logo.svg`（如果有的話）
4. 上傳完成後，確認檔案結構如下：

```
public_html/
├── index.html
├── assets/
│   ├── index-xxxxx.css
│   ├── index-xxxxx.js
│   └── skill_hub_logo-xxxxx.svg
```

#### Step 5: 驗證部署

1. 前往你的網域（例如：`https://yourdomain.com`）
2. 應該會看到 Discord 廣告生成器的首頁
3. 測試所有功能是否正常運作

---

### 方法二：使用 FTP（進階用戶）

#### Step 1: 取得 FTP 資訊

1. 在 Hostinger hPanel 中找到 **FTP 帳號**
2. 記下以下資訊：
   - FTP 主機：`ftp.yourdomain.com`
   - 使用者名稱：`u123456789` 或你的帳號
   - 密碼：你設定的密碼
   - 埠號：`21`

#### Step 2: 使用 FTP 客戶端

推薦使用 **FileZilla**（免費）：

1. 下載並安裝 [FileZilla](https://filezilla-project.org/)
2. 開啟 FileZilla
3. 輸入 FTP 資訊並連線
4. 在右側（遠端站台）找到 `public_html` 資料夾
5. 在左側（本機）找到你的 `dist` 資料夾
6. 將 `dist` 內的所有檔案拖曳到右側的 `public_html` 資料夾

#### Step 3: 驗證部署

前往你的網域確認部署成功。

---

### 方法三：使用 Git 部署（最進階）

#### Step 1: 在 Hostinger 啟用 SSH

1. 在 hPanel 中找到 **進階 (Advanced)** → **SSH 存取**
2. 啟用 SSH 存取

#### Step 2: SSH 連線到伺服器

```bash
ssh u123456789@yourdomain.com
```

#### Step 3: 部署專案

```bash
# 進入 public_html 資料夾
cd public_html

# 清空現有檔案
rm -rf *

# 使用 Git clone 你的專案（如果有推送到 GitHub）
git clone https://github.com/yourusername/discord_ad_generator.git temp
cd temp

# 安裝依賴並建置
npm install
npm run build

# 將建置檔案移到 public_html
mv dist/* ../
cd ..
rm -rf temp
```

---

## ⚙️ 環境變數設定

### 重要：環境變數處理

由於 Vite 在建置時會將環境變數打包到 JavaScript 檔案中，你需要在**建置前**確保 `.env` 檔案設定正確。

#### Step 1: 檢查 .env 檔案

確保 `.env` 包含所有必要的變數：

```env
# Gemini API
VITE_NANO_BANANA_API_KEY=your_actual_api_key

# Firebase 配置
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Discord Webhook URL（選填）
VITE_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
```

#### Step 2: 重新建置

如果環境變數有任何修改，需要重新建置：

```bash
npm run build
```

然後再次上傳 `dist` 資料夾內容到 Hostinger。

---

## 🔒 安全性建議

### 1. 保護 API 金鑰

⚠️ **注意**：由於這是純前端專案，所有環境變數會被打包到 JavaScript 中，使用者可以在瀏覽器中查看。

**建議做法**：

- 對於 Gemini API Key：設定 API 金鑰的網域白名單限制
- 對於 Firebase：在 Firebase Console 設定網域限制
- 對於敏感操作：考慮建立後端 API 來處理敏感請求

### 2. 設定 Firebase Security Rules

在 Firebase Console 中設定安全規則：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 僅允許已驗證使用者讀寫
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

---

## 🐛 常見問題排除

### 問題 1: 頁面顯示空白

**原因**：路徑問題

**解決方案**：

1. 檢查 `vite.config.js` 的 `base` 設定
2. 如果網站在子目錄（如 `yourdomain.com/app`），需要設定：

```javascript
// vite.config.js
export default defineConfig({
  base: '/app/' // 改為你的子目錄
  // ...
})
```

### 問題 2: 圖片或 CSS 無法載入

**原因**：檔案路徑錯誤或檔案未上傳

**解決方案**：

1. 確認 `assets` 資料夾已完整上傳
2. 檢查 File Manager 中的檔案結構
3. 確認檔案權限（應為 644）

### 問題 3: API 請求失敗

**原因**：CORS 或環境變數問題

**解決方案**：

1. 檢查瀏覽器 Console 的錯誤訊息
2. 確認環境變數已正確設定並重新建置
3. 檢查 Firebase、Gemini API 的網域白名單設定

### 問題 4: Firebase 連線失敗

**原因**：Firebase 配置錯誤

**解決方案**：

1. 在 Firebase Console 中檢查專案設定
2. 確認網域已加入授權網域清單：
   - Firebase Console → Authentication → Settings → Authorized domains
   - 新增你的 Hostinger 網域

---

## 📱 測試清單

部署完成後，請測試以下功能：

- [ ] 頁面正常顯示
- [ ] 表單輸入正常
- [ ] 日期選擇器正常運作
- [ ] 檔案上傳功能正常
- [ ] 圖片生成功能正常
- [ ] Discord 發布功能正常
- [ ] 歷史記錄功能正常
- [ ] 響應式設計（手機、平板）正常

---

## 🔄 更新部署流程

當你修改程式碼後需要更新網站：

### 快速更新步驟

```bash
# 1. 重新建置
npm run build

# 2. 上傳 dist 資料夾內容到 Hostinger
# 使用 File Manager 或 FTP 客戶端覆蓋 public_html 中的檔案

# 3. 清除瀏覽器快取
# Ctrl + Shift + R (Windows) 或 Cmd + Shift + R (Mac)
```

### 建議使用版本控制

```bash
# 建立版本標籤
git tag v1.0.0
git push origin v1.0.0

# 在 SPEC/MAIN.md 中記錄更新
```

---

## 📞 需要協助？

如果遇到問題：

1. 檢查 Hostinger 的[知識庫](https://support.hostinger.com/)
2. 查看瀏覽器 Console 的錯誤訊息
3. 確認 Firebase 和 Gemini API 的設定
4. 檢查 Network 面板確認 API 請求狀態

---

## ✅ 部署成功！

恭喜！你的 Discord 廣告生成器已成功部署到 Hostinger。

**下一步**：

- 測試所有功能
- 設定自訂網域（如需要）
- 設定 SSL 憑證（Hostinger 通常自動提供）
- 分享給你的團隊使用

---

**最後更新**: 2025-12-04  
**版本**: v4.3.0
